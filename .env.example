# 环境变量配置示例
# 复制此文件为 .env.development 并填入实际值

# GitHub OAuth App 的 Client ID（必填）
# 获取方式：https://github.com/settings/developers
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub 仓库名称（可选，默认为 jisuye-ext-data）
VITE_GITHUB_REPO_NAME=jisuye-ext-data

# OAuth 代理 URL（推荐用于生产环境，解决 CORS 问题）
# 如果不配置，会直接请求 GitHub 的 access_token 端点（可能在自定义域名触发 CORS）
# 获取方式：部署 cloudflare-worker.js 到 Cloudflare Workers
VITE_OAUTH_PROXY_URL=https://your-worker.workers.dev

# 开发模式配置（仅 .env.development）
VITE_DEV_MODE=true
VITE_DEV_DATA_PATH=./dev-data

